TODO:
- Check that everything runs.
- Check that the changes to include more than one document per role went through globally.
- Integrate disclosure.py into the regular fetcher.
- Integrate the Polisis code into the regular code.
- Integrate render command into other commands.
- Double-check that the Polisis models are always used consistently (to verify results). 
    - Make coverage based off Polisis determinations on global document sets.
- Double-check that rendered pages are handled sensibly in the global project flow.
- Remove one of the two Play store data sharing pages from the doc set.
- Investigation into which caps can be dropped.
- See which service uses can be dropped.